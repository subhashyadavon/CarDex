namespace CarDexBackend.Domain.Enums
{
    public enum RewardEnum
    {
        PACK,                   // Reward is a pack
        CURRENCY,               // Reward is in-game currency
        CARD_FROM_TRADE,        // Reward is a card received from trade
        CURRENCY_FROM_TRADE     // Reward is currency received from trade
    }
}
